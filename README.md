# CRUD-API
A small in-memory CRUD API for managing a to-do list, built with Python &amp; FastAPI. Data lives only in memory (a Python list) and it resets whenever the server restarts. That's intentional at this stage.
